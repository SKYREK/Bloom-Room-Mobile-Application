<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- ... other style attributes ... -->
    </style>

    <!-- Custom style for the BottomNavigationView -->
    <style name="AppTheme.BottomNav" parent="Widget.Design.BottomNavigationView">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:theme">@style/BottomNavThemeOverlay</item>
    </style>

    <style name="BottomNavThemeOverlay">
        <item name="android:colorForeground">@android:color/transparent</item>
    </style>

    <style name="Widget.Theme.BloomRoom.ActionBar.Fullscreen" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="Widget.Theme.BloomRoom.ButtonBar.Fullscreen" parent="">
        <item name="android:background">@color/black_overlay</item>
        <item name="android:buttonBarStyle">?android:attr/buttonBarStyle</item>
    </style>
</resources>
